/**
 * Database Seeder Runner
 * This script runs the seedDatabase.js file to populate the database with initial data
 */

// Run the seed script
require('./src/utils/seedDatabase');

console.log('Starting database seeding process...');
